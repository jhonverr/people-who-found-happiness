# Environment Configuration Template
# 환경 변수 설정 템플릿

# 참고: 현재 이 프로젝트는 환경 변수를 사용하지 않습니다.
# 향후 API 키나 설정이 필요한 경우 아래 형식으로 추가하세요.

# Example: Google Analytics Tracking ID
# VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X

# Example: Contact Form API Endpoint
# VITE_CONTACT_API_URL=https://api.example.com/contact

# Example: Custom API Base URL
# VITE_API_BASE_URL=https://api.example.com

# 사용 방법:
# 1. 이 파일을 .env.local 로 복사
# 2. 실제 값으로 변경
# 3. 코드에서 import.meta.env.VITE_변수명 으로 접근
# 4. .env.local 파일은 절대 Git에 커밋하지 마세요!
